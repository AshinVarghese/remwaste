import React from "react";

const Header: React.FC = () => (
  <header className="mb-4">
    <h1 className="text-2xl font-bold text-gray-800">
      Skip Selection Dashboard
    </h1>
  </header>
);

export default Header;
